CREATE TABLE game3( device_id integer DEFAULT 0, name text DEFAULT "ChotaKutta", session text DEFAULT "INITIAL", type text DEFAULT "number", duration integer NOT NULL, scoore integer NOT NULL, timeofday int NOT NULL, PRIMARY KEY(timeofday));